<h1>Student List</h1>

<% @students.each do |student| %>
  <p>
    <%= link_to student.name.capitalize, student_path(student)%>
  </p>
<% end %>

  <p>
    <%= link_to "Add a student", new_student_path %>
  </p>
  <p>
    <%= link_to "See courses", courses_path %>
  </p>

