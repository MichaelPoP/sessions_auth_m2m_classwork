<h1>More info about <%= @student.name%></h1>

<h2>Enrollments</h2>
<% @student.courses.each do |course|%>
  <p>
    Name: <%= link_to course.name.capitalize, course_path(course) %>
  </p>
<% end %>

<%= link_to("Enroll in courses", new_enrollment_path) %>

<br>
<%= link_to("Edit this student", edit_student_path)%>
<br>

<%= link_to "See courses", courses_path %>
<br>
<%= link_to "See students", students_path %>
<p>
  <%= link_to("Delete this student", student_path, class: "btn btn-danger",method: "delete", data: {confirm: "Are you sure?"})%>
</p>