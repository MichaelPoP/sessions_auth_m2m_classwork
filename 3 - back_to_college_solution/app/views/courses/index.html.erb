<h1>Course List</h1>
  <p>
    <%= link_to "Add a new course", new_course_path%>
  </p>
    <% @courses.each do |course| %>
  <p>
      <%= link_to course.name.capitalize, course_path(course.id) %>
  </p>
<% end %>

  <p>
    <%= link_to "See students", students_path %>
  </p>
